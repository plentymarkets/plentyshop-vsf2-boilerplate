on:
  pull_request:
    types: [opened]
jobs:
  request:
    name: Request reviews on opened PRs
    runs-on: ubuntu-latest

    steps:
      - name: Create PR review request
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr edit ${{ github.event.pull_request.html_url }} \
              --add-reviewer @plentymarkets/shop-front-end

on:
  pull_request:
    types: [review_requested]
jobs:
  notify:
    runs-on: ubuntu-latest
    name: notify chat
    steps:
    - name: Google Chat Notification
      uses: Co-qn/google-chat-notification@releases/v1
      with:
        name: Pull request review draft
        url: https://chat.googleapis.com/v1/spaces/AAAA_iUfT18/messages?key=AIzaSyDdI0hCZtE6vySjMm-WEfRq3CPzqKqqsHI&token=Qnrj63BATEim_jN7dwfOIg_dS8AB78TgZQTsRBDIJ5g%3D
        status: ${{ job.status }}
      if: always()
     